---

const { tags } = Astro.props;
---

<div class="text-center grid grid-cols-3 gap-2">
  {tags.map((tag: string) => 
    <a class="bg-blue-100 text-blue-800 hover:bg-blue-300 text-xs font-medium px-2.5 py-0.5 rounded" href={`/tags/${tag}`}>{tag}</a>
  )}
</div>